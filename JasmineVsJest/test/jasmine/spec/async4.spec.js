describe("Suit4", function () {
  it("should return Done", async function () {
    const result = await waitTime();
    expect(result).toBe("Done");
  });

  it("should return Done", async function () {
    const result = await waitTime();
    expect(result).toBe("Done");
  });

  it("should return Done", async function () {
    const result = await waitTime();
    expect(result).toBe("Done");
  });
  it("should return Done", async function () {
    const result = await waitTime();
    expect(result).toBe("Done");
  });
});
