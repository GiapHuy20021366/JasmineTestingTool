const waitTime = require("../../src/async");

test("should return done", async () => {
  const result = await waitTime();
  expect(result).toBe("Done");
});

test("should return done", async () => {
  const result = await waitTime();
  expect(result).toBe("Done");
});

test("should return done", async () => {
  const result = await waitTime();
  expect(result).toBe("Done");
});

test("should return done", async () => {
  const result = await waitTime();
  expect(result).toBe("Done");
});
